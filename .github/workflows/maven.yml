# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: inspect 1
      run: |
        pwd
        ls -al
        declare -p

#    - uses: actions/checkout@v4
#    - name: Set up JDK 17
#      uses: actions/setup-java@v4
#      with:
#        java-version: '17'
#        distribution: 'temurin'
#        cache: maven
#    - name: Set up Maven
#      uses: stCarolas/setup-maven@v5
#      with:
#        maven-version: 3.9.6
#    - name: Build with Maven
#      run: mvn -B package --file pom.xml
#
#    - name: Upload p2.index
#      uses: actions/upload-artifact@v4
#      with:
#        name: p2.index
#        path: sites/org.eclipse.tea.repository/target/repository/p2.index
#    - name: Upload artifacts.jar
#      uses: actions/upload-artifact@v4
#      with:
#        name: artifacts.jar
#        path: sites/org.eclipse.tea.repository/target/repository/artifacts.jar
#    - name: Upload artifacts.xml.xz
#      uses: actions/upload-artifact@v4
#      with:
#        name: artifacts.xml.xz
#        path: sites/org.eclipse.tea.repository/target/repository/artifacts.xml.xz

    # Optional: Uploads the full dependency graph to GitHub to improve the quality of Dependabot alerts this repository can receive
    # - name: Update dependency graph
    #  uses: advanced-security/maven-dependency-submission-action@571e99aab1055c2e71a1e2309b9691de18d6b7d6
